name: Build Nodus for Windows

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build Nodus
        shell: cmd
        run: |
          if not exist bin mkdir bin
          cl /nologo /O2 /W4 /D_CRT_SECURE_NO_WARNINGS /Isrc /FIframe_override.h src\main.c user32.lib gdi32.lib /link /SUBSYSTEM:WINDOWS /OUT:bin\Nodus.exe

      - name: Package
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item bin\Nodus.exe dist\Nodus.exe
          Copy-Item README.md dist\README.md
          Compress-Archive -Path dist\Nodus.exe,dist\README.md -DestinationPath Nodus-Windows.zip -Force

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: Nodus-Windows
          path: Nodus-Windows.zip
          retention-days: 30
